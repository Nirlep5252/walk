# Walk 0.5.1

- Adds an emoji and symbol picker with `emoji`, `em`, `symbol`, `sym`, and `:` searches.
- Shows emoji and symbols in the result icon slot instead of duplicating glyphs in result titles.
- Uses cached Twemoji image assets for emoji rendering.
- Adds copy actions for characters and Unicode code points.
