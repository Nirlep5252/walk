# Walk 0.3.12

- Restores standard Windows installer generation so GitHub releases include `Walk-win-Setup.exe` again.
- Keeps the code-level browser false-positive mitigations from recent patches: no self-launched `What's New` helper, no runtime release-note recovery, and no ReadyToRun in release builds.
