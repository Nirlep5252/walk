# Walk 0.3.14

- Fixes bundled file search on clean machines by explicitly indexing common user folders such as `Downloads`, `Documents`, `Desktop`, `Pictures`, `Music`, and `Videos`.
- Forces a bundled Everything rescan on startup so users who already updated to `0.3.13` rebuild their file index automatically.
