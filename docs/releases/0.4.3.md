# Walk 0.4.3

- Adds a dedicated "Search the web in `<default browser>`" result directly after app matches.
- Opens real URLs directly in the default browser instead of treating them like search terms.
- Treats non-URL text as a web search query and routes it through the default browser.
