# Walk 0.4.10

- Fixes `Win+Space` so typing after opening Walk no longer behaves as if the Windows key is still held.
- Keeps the Start menu suppressed without swallowing the Windows key release event.
