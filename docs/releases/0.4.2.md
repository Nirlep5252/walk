# Walk 0.4.2

- Prioritizes app results ahead of file results so launcher searches surface installed apps first.
- Keeps the same app-first ordering while results stream in, preventing file matches from briefly hiding app matches.
- Adds query router coverage for app-vs-file ordering in both full and incremental search paths.
