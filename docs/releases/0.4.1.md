# Walk 0.4.1

- Adds a second launcher results view with a thumbnail-first grid alongside the original list view.
- Preserves original thumbnail aspect ratios and adds `Ctrl+G` to toggle between list and grid mode.
- Reworks grid rendering so full result sets stay responsive without cropping previews or stalling on file thumbnails.
