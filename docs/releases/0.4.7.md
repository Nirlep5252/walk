# Walk 0.4.7

- Fixes search-result flicker while typing by coalescing visible result updates for each query.
- Keeps the search activity indicator layout-stable so the view controls no longer shift when searches start or finish.
