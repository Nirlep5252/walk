# Walk 0.4.0

- Reworks launcher search so fast results appear immediately while slower providers keep filling in.
- Fixes file-search hangs and crashes caused by the new incremental search pipeline running heavy work on the UI thread.
- Restores explicit file queries such as `steam *.mp4` so wildcard searches stream file matches again instead of dropping them.
- Improves Everything-backed file search to stream direct matches earlier, then continue with fuzzy matches afterward.
