# Walk 0.3.9

- Removes the new self-launched `What's New` helper path and keeps the changelog viewer in-process.
- Drops runtime release-note recovery from GitHub so the app no longer fetches tagged markdown on demand.
- Makes unsigned Windows releases safer to distribute by disabling ReadyToRun and omitting `Setup.exe` from uploaded assets when signing is not configured.
