# Walk 0.3.7

- Always checks for updates on launch instead of waiting after startup.
- Blocks installs that do not include release notes, so every update has a mandatory changelog.
- Shows a native `What's New` window after updates and adds a tray entry to reopen the latest changelog.
- Fixes the tray `What's New` action by launching the changelog window in a dedicated dialog mode.
