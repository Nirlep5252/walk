# Walk 0.3.8

- Fixes the `What's New` window for users who updated directly from 0.3.6 to 0.3.7.
- Recovers missing changelog content by fetching the current installed version's tagged release notes when local state is empty.
- Keeps the dedicated `What's New` dialog launcher and tray entry working with the recovered release notes.
