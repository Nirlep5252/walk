# Walk 0.4.9

- Allows `Win+Space` to be used as Walk's global launcher hotkey by falling back to a keyboard hook when Windows refuses the shortcut.
- Captures Windows-key shortcuts correctly in Settings while recording a new launcher hotkey.
