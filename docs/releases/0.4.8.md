# Walk 0.4.8

- Restores live incremental search updates while keeping UI refreshes coalesced for smooth typing.
- Fixes file search when running Debug builds through `dotnet Walk.dll`.
- Disables Debug apphost generation to avoid local Defender false positives on generated `Walk.exe`.
