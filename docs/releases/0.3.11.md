# Walk 0.3.11

- Fixes unsigned release publishing by skipping Velopack installer generation instead of generating `Setup.exe` and deleting it later.
- Keeps the browser false-positive mitigation changes from `0.3.10`: no self-launched `What's New` helper, no runtime release-note recovery, and no ReadyToRun in release builds.
