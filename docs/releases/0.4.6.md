# Walk 0.4.6

- Adds snappy movement-based launcher animations for opening, result selection, row entry, grid cards, and list/grid switching.
- Refines launcher spacing and the compact list/grid segmented control so the search UI feels better aligned.
- Reworks launcher icons to use crisp WPF vector paths from Fluent SVG data and prevents fallback icons from overlapping real app icons.
