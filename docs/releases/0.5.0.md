# Walk 0.5.0

- Adds clipboard history for recently copied text, file lists, and images.
- Adds explicit `clip`, `clipboard`, and `cb` searches with copy, pin/unpin, and delete actions.
- Stores copied images as local PNG history entries with preview support in grid results.
- Adds a Settings toggle for Clipboard History.
