# Walk 0.3.15

- Fixes a bundled `Everything` config formatting bug that broke folder indexing on some machines after updating to `0.3.14`.
- Rewrites the bundled search config correctly on startup so affected users rebuild their file index automatically.
