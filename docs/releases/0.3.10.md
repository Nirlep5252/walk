# Walk 0.3.10

- Fixes the GitHub Actions release workflow so patch tags create release jobs again.
- Keeps the `0.3.9` browser false-positive mitigation changes: no self-launched `What's New` helper, no runtime release-note fetch recovery, and safer unsigned release packaging.
