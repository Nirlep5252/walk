<Window
    x:Class="Walk.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:Walk.Helpers"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Settings"
    Width="480"
    Height="344"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    ShowInTaskbar="False"
    Background="{DynamicResource ApplicationBackgroundBrush}">

    <Window.Resources>
        <helpers:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock
                Text="Settings"
                FontSize="28"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,24,0,0" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="Launcher Hotkey"
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <Grid
                Grid.Row="0"
                Grid.Column="1"
                HorizontalAlignment="Right">
                <Button
                    x:Name="RecordHotkeyButton"
                    MinWidth="220"
                    Padding="14,10,48,10"
                    HorizontalContentAlignment="Left"
                    Command="{Binding StartRecordingCommand}">
                    <DockPanel LastChildFill="True">
                        <ui:SymbolIcon
                            DockPanel.Dock="Left"
                            Symbol="Keyboard24"
                            Margin="0,1,10,0"
                            Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" />
                        <TextBlock
                            Text="{Binding HotkeyButtonText}"
                            FontSize="15"
                            FontWeight="SemiBold"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    </DockPanel>
                </Button>
                <Button
                    Width="30"
                    Height="30"
                    Margin="0,0,9,0"
                    Padding="0"
                    Panel.ZIndex="1"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Command="{Binding ResetHotkeyCommand}"
                    ToolTip="Reset to Alt+Space"
                    Visibility="{Binding ShouldShowResetHotkey, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:SymbolIcon
                        Symbol="ArrowCounterclockwise24"
                        FontSize="14"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </Button>
            </Grid>

            <TextBlock
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,8,0,0"
                Text="{Binding StatusMessage}"
                TextWrapping="Wrap"
                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                Visibility="{Binding StatusMessage.Length, Converter={StaticResource CountToVisibilityConverter}}" />

            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                Margin="0,24,0,0"
                VerticalAlignment="Top"
                Text="Auto Start on Login"
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <StackPanel
                Grid.Row="2"
                Grid.Column="1"
                Margin="0,24,0,0"
                HorizontalAlignment="Right">
                <ui:ToggleSwitch
                    IsChecked="{Binding AutoStartOnLogin}"
                    OffContent="Off"
                    OnContent="On" />
            </StackPanel>
        </Grid>

        <Grid
            Grid.Row="2"
            Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="{Binding DisplayVersion}"
                Foreground="{DynamicResource TextFillColorTertiaryBrush}" />

            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                HorizontalAlignment="Right">
                <Button
                    MinWidth="92"
                    IsCancel="True"
                    Command="{Binding CancelCommand}"
                    Content="Cancel" />
                <Button
                    MinWidth="92"
                    Margin="12,0,0,0"
                    IsDefault="True"
                    Command="{Binding SaveCommand}"
                    Content="Save" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
